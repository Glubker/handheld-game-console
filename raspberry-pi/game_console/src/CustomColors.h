#ifndef CUSTOM_COLORS_H
#define CUSTOM_COLORS_H

#include "raylib.h"

// Define custom colors
const Color CUSTOM_BACKGROUND = { 15, 15, 15, 255 };             // Primary background (very dark gray)
const Color CUSTOM_SECONDARY_BACKGROUND = { 25, 25, 25, 255 };   // Secondary background (dark gray)
const Color CUSTOM_BORDER_COLOR = { 50, 50, 50, 255 };           // Border color (medium-dark gray)
const Color CUSTOM_TEXT_COLOR = { 200, 200, 200, 255 };          // Text color (light gray)
const Color CUSTOM_SELECTED_TEXT_COLOR = { 255, 255, 255, 255 }; // Selected text color (white)
const Color CUSTOM_SELECTED_BACKGROUND_COLOR = { 45, 45, 45, 255 }; // Selected background (medium-dark gray)
const Color CUSTOM_LIGHT_GRAY = { 150, 150, 150, 255 };          // Light gray for UI elements
const Color CUSTOM_DARK_GRAY = { 30, 30, 30, 255 };              // Dark gray for backgrounds
const Color CUSTOM_BLUE = { 0, 122, 255, 255 };                  // Blue for accent color
const Color CUSTOM_RED = { 194, 54, 22, 255 };                   // Red for off toggles (muted red)
const Color CUSTOM_WHITE = { 255, 255, 255, 255 };               // White for handles and text

#endif // CUSTOM_COLORS_H
